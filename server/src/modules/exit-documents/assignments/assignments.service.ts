// Superseded by server/src/modules/exit-clearance/assignments/exit-clearance.service.ts
// — see schema.prisma's "EXIT CLEARANCE WORKFLOW" module note. Left as an
// intentional no-op (see assignments.module.ts's comment) rather than
// deleted — the ExitDocumentAssignment Prisma model this depended on no
// longer exists.
export {}
