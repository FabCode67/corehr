// Superseded by the configurable Exit Clearance Workflow — see
// server/src/modules/exit-clearance/assignments and schema.prisma's "EXIT
// CLEARANCE WORKFLOW" module note. Left as an intentional no-op (not
// imported by app.module.ts) rather than deleted — the sandbox this was
// authored in couldn't run shell commands to remove the folder. Safe to
// delete this whole exit-documents/ folder.
export {}
