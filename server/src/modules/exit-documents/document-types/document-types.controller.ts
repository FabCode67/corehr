// Superseded by the configurable Exit Clearance Workflow — see
// server/src/modules/exit-clearance/templates and schema.prisma's "EXIT
// CLEARANCE WORKFLOW" module note. Left as an intentional no-op (see
// document-types.module.ts's comment) rather than deleted.
export {}
