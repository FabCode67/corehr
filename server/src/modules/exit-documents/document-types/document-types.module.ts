// Superseded by the configurable Exit Clearance Workflow — see
// server/src/modules/exit-clearance and schema.prisma's "EXIT CLEARANCE
// WORKFLOW" module note. The ExitDocumentType/ExitDocumentAssignment
// Prisma models this module was built on were dropped by the
// 20260913180000_add_exit_clearance_workflow migration, so this file is now
// an intentional no-op (not imported by app.module.ts). Left in place
// rather than deleted because the sandbox this was authored in couldn't run
// shell commands at the time — safe to delete this whole exit-documents/
// folder.
export {}
