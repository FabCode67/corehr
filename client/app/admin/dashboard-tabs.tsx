// Deprecated — the Overview/Executive Summary/HR Analytics tab bar this file
// defined is gone; all three were consolidated into a single HR Analytics
// page per request ("keep the content only in HR Analytics"). Nothing
// imports this file anymore. It's left as an inert stub rather than deleted
// because the sandbox this edit was made in can't remove files on this
// mount — see app/admin/page.tsx and app/admin/executive-dashboard/page.tsx
// for where the old routes now redirect to.
export {}
