import { ComingSoon } from "@/components/portal/coming-soon"

export default function AdminSettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Roles & permissions, approval workflow configuration, form builder, salary grade references, and system settings."
    />
  )
}
