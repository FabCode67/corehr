// Superseded by ./exit-clearance.ts — see schema.prisma's "EXIT CLEARANCE
// WORKFLOW" module note. The /exit-documents/* backend routes this called
// no longer exist (server/src/modules/exit-documents is now a no-op). Left
// as an intentional no-op rather than deleted; safe to remove this file.
export {}
